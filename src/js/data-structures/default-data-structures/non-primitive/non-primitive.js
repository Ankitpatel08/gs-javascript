/*
* Data Structures - allow you to manage data
* Arrays: [1,2,3] | Ordered List of data | duplicate allowed
* Objects: {name: 'Ankit'} | Key:value pair of unordered data
* Maps: new Map();map.set('a','b') | key: value pairs of ordered, iterable data
* Sets: new Set(); set.add(1) | Unordered List | No Duplicate allowed
*/

/**
 * WeakSet | WeakMap: Variatons of Set and Map
 * Values and keys are only 'Weakly Referenced'
 * Garbage collection can delete keys and values if not used anywhere in the app
 */
import './arrays';
import './sets';
import './objects';
import './maps';